const myStates = [
    "telangana",
    "delhi",
    "odisha",
    "karnataka",
    "telangana"
]
 /* var updated = myStates.splice(0, 2) // 0 is from where to start  and 2 is how many elements
console.log(updated)  */ // it reutrns ['telanagana',' delhi']

/* var updated = myStates.splice(2, 2)//it is going to return from 2 and it also returns more 1  element total 2 as we gave 2 in the second parameter
console.log(updated) */